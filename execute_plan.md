# Execute Plan — Computer Vision Assignment (Delta Reality)

Living document. Updated as work proceeds. Reasoning, debug notes, and
decisions go here so the final state explains both *what* was done and *why*.

---

## 1. Problem statement (rephrased)

We are given an assignment package that contains:

- A Unity viewer application (Linux x86_64 build).
- Three point clouds: `image1.ply`, `image2.ply`, `image3.ply`. Each is the
  point cloud generated from one monocular RGB image (we also have the source
  `.png`, a depth map `_depth.png`, and a rays/normals visualization
  `_rays.png` for each).
- A trajectory file `traj.txt` with **three** rows. Each row encodes the
  *world-space camera pose* for the corresponding image.

The systems that produced the data and the systems that built the viewer use
**different coordinate conventions**. The viewer therefore renders the raw
data in the wrong place / wrong orientation. Our job is to figure out the
transformation between the two conventions and emit corrected replacement
files:

- `image1.ply`, `image2.ply`, `image3.ply` — point clouds in the viewer's
  world space.
- `traj.txt` — camera poses in the viewer's world space.

The PDF intentionally does **not** specify the difference between the two
coordinate systems; finding it is part of the task.

When everything is right, the viewer should show a reconstructed indoor scene
similar to the reference screenshot in the PDF (room with cabinets / desk
chair / a window).

---

## 2. What's in the data on disk

```
ComputerVisionAssignment_Data/StreamingAssets/
├── Points/
│   ├── image1.ply           # ~125 MB, ASCII PLY, 2,963,610 vertices, xyz + rgb (uchar)
│   ├── image1.png           # source RGB
│   ├── image1_depth.png     # depth (visualization)
│   ├── image1_rays.png      # per-pixel ray directions visualization
│   ├── image2.ply           # same vertex count (2,963,610)
│   ├── image2.png
│   ├── image2_depth.png
│   ├── image2_rays.png
│   ├── image3.ply           # same vertex count (2,963,610)
│   ├── image3.png
│   ├── image3_depth.png
│   └── image3_rays.png
└── traj.txt                 # 3 rows, each row = index + 16 floats (4x4 matrix, row-major)
```

The fact that all three PLYs have exactly the same vertex count
(2,963,610 = ~1721 × ~1722, close to a 1920×1545-ish grid — actually
`2963610 = 1722 × 1721`, very close) strongly suggests the points were
generated by **back-projecting every pixel** of the source RGB image through
a depth/rays map. So each PLY is in the **camera-local frame** of its image,
not pre-transformed into the world.

`traj.txt` example — first row:

```
1   0.6235648  0.7751530 -0.1015122  3.9695432
    0.7817087 -0.6198837  0.0683796 -3.2100663
   -0.0099211 -0.1219921 -0.9924815 -1.4454179
    0.0        0.0        0.0        1.0
```

That's a 4×4 row-major homogeneous matrix. The upper-left 3×3 is a rotation
(rows/cols are orthonormal up to fp noise), the right column is a 3-D
translation, the last row is `[0 0 0 1]`. **3 rows → 3 cameras → 3 PLY
files**, one-to-one correspondence.

The matrix maps **camera → world**: column 4 is the camera position in world
space (~ (3.97, -3.21, -1.45) for image 1), and the 3×3 block is the camera's
orientation. (Verified — see §4 once we run the analysis script.)

---

## 3. Why this is mostly a coordinate-frame problem

Two transformations are *both* in play and we need to combine them:

1. **PLY points are in camera-local space.** The viewer's job, given a
   per-camera pose, would normally be to place each cloud at its camera's
   world position. But the assignment says we should emit replacement PLYs
   that "display correctly" — meaning the viewer is treating PLYs as already
   being in world space. So we must **bake the camera-to-world transform
   into the point coordinates** before writing them out.

2. **Source vs. viewer coordinate handedness.** The data is almost certainly
   from a CV/photogrammetry pipeline (depth + rays + PLY), so it's most
   likely:
   - **OpenCV / COLMAP camera convention:** X right, Y down, Z forward
     (right-handed).
   The viewer is Unity, which uses:
   - **Unity world convention:** X right, Y up, Z forward (**left-handed**).
   So we expect a handedness flip — typically negate Y (and adjust the
   rotation accordingly) — between source and viewer.

Note the structure of the first pose's 3rd row: `(-0.0099, -0.1220, -0.9925)`.
That row is almost `(0, 0, -1)`, i.e. the world-frame Z axis of the camera
points roughly down the -Z world axis. That is consistent with the data being
in a **Y-up world (with cameras looking roughly horizontally)** and Z-forward
*camera* — basically OpenCV-style camera-from-world, possibly already
inverted. We'll confirm experimentally.

---

## 4. Plan of attack

Concrete steps, in order:

1. **Set up env** — conda env `cv_assignment` with `numpy`, `plyfile`,
   `pillow`, `scipy`. (Done.)
2. **Write `scripts/analyze.py`** that:
   - Loads `traj.txt`, checks each 3×3 is a proper rotation (`det = +1`).
   - Streams each PLY, reports bbox, centroid, and mean color.
   - Decides: is the translation column the camera position in world (cam→world)
     or the world origin in camera (world→cam)?
3. **Hypothesis test for the world transform.** Two candidates:
   - H1: PLY is camera-local; apply `T_cw` (cam→world) to get world points.
   - H2: PLY is already partly world but in wrong handedness.
   We'll project a few characteristic features (e.g., the centroid, the
   axis-aligned bbox corners) into world space under H1 and see if all three
   cameras converge to a *single coherent room* (their world-space bboxes
   overlap and form a contiguous indoor scene). If yes → H1 is correct.
4. **Handedness fix.** Apply the candidate flip (e.g. `Y → -Y` on both the
   point cloud and the camera pose). Re-emit `traj.txt` with the corrected
   pose matrices, even though the points are already baked into world — the
   viewer may still draw camera gizmos / use it for the initial view.
5. **Write `scripts/transform.py`** — the production script that:
   - Reads each PLY (streaming, since they're ~125 MB ASCII).
   - For each point: `p_world = T_cw · p_cam`, then apply handedness flip
     `S = diag(1, -1, 1)` (best guess for OpenCV→Unity).
   - Writes new binary-little-endian PLY to `data/output/imageN.ply` to keep
     file size down (the viewer should accept both ASCII and binary — verify).
   - Writes `data/output/traj.txt` with each pose mapped through the same
     handedness conjugation: `T'_cw = S · T_cw · S` (and the camera rotation
     itself goes through the OpenCV→Unity camera-axis flip).
6. **Validate** — we *cannot* run the Linux viewer on this macOS host. Flag
   that explicitly. Programmatic checks instead:
   - All three world-space PLYs should share a roughly common bbox volume
     (the room).
   - The transformed camera positions should sit *inside* that bbox or
     immediately adjacent to it, with cameras looking *into* the scene.
   - A sanity render of a downsampled cloud using matplotlib/Open3D-on-Mac
     would help; Open3D works on Apple Silicon.
7. **Document & commit.** Frequent small commits. README has full instructions.

---

## 5. Why we can't fully validate on this machine

The viewer ships as a Linux x86_64 Unity build (`ComputerVisionAssignment.x86_64`
+ `UnityPlayer.so` + `libdecor-*.so*`). This host is macOS (Darwin 24.6, arm64).
We can't natively run that binary. We will:

- Do all transformation work and programmatic validation here.
- Document exactly how the user reproduces the result on a Linux machine
  (run `python scripts/transform.py`, drop the outputs into
  `ComputerVisionAssignment_Data/StreamingAssets/` and
  `.../Points/`, then launch `./ComputerVisionAssignment.x86_64`).

---

## 6. Log (chronological)

- **2026-05-30** — Read assignment PDF. Inventoried files. Confirmed each PLY
  has 2,963,610 vertices and trajectory has 3 poses. Sketched the
  hypothesis: camera-local PLY + cam→world pose + handedness flip
  (OpenCV/right-handed → Unity/left-handed). Set up conda env
  `cv_assignment` (python 3.11, numpy, plyfile, pillow, scipy) and a git
  repo on `main`.

- **2026-05-30** — Wrote `scripts/analyze.py`. Initial parse failure: I had
  assumed `traj.txt` rows start with an image index, but they don't — that
  was a line-number artifact from the file viewer. Fixed: rows are just 16
  floats; image index is implicit (row N → imageN.ply). Re-ran. Findings:

  - All three 3×3 rotation blocks are proper rotations (`det = +1`,
    `‖R Rᵀ − I‖∞ < 5e-8`).
  - PLY local Z bounds are **all strictly positive**:
    image1 z∈[+1.42, +9.82], image2 z∈[+1.48, +2.46], image3 z∈[+1.38, +3.51].
    That is the signature of points expressed in a **camera frame with +Z =
    forward (away from the camera)** — i.e. OpenCV / COLMAP camera
    convention. The PLYs are *camera-local*, not world.
  - All three cameras are clustered tightly in world space:
    `(3.97, −3.21, −1.45)`, `(4.52, −3.15, −1.40)`, `(4.71, −3.12, −1.40)`.
    X spread 0.74 m, Y spread 0.09 m, Z spread 0.05 m. Consistent with one
    person taking three shots from nearly the same spot while rotating —
    not three different rooms.
  - Camera `+X` axes in world have `z ≈ 0` for all three cameras
    (`-0.010, −0.006, −0.022`). Camera `+Z` axes are all `≈ (~0, ~0, −0.99)`.
    Interpretation: the cameras' image-right is horizontal in world space,
    and the cameras' look-direction is along world `−Z`. That is exactly
    what we'd expect for **world = Y-up, right-handed** with cameras shot
    horizontally facing the world's `−Z` half-space.
  - H1 test: applied each `T_cw` to the 8 bbox corners of the corresponding
    local PLY. Pairwise overlap of resulting **world** bboxes:
    `image1 ∩ image2 = 11.5`, `image1 ∩ image3 = 35.3`, `image2 ∩ image3 = 11.5`
    cubic units. They overlap substantially → all three views share the
    same world region → **PLYs are camera-local; H1 (apply `T_cw`) is
    correct.**

  Conclusion on source convention: **right-handed, Y-up world; OpenCV-style
  camera frame.**

## 7. Picking the source→viewer transformation

The viewer is a Unity app, which uses a **left-handed Y-up** world (X right,
Y up, Z forward). The conventional way to convert a right-handed Y-up scene
to Unity is to **negate one of the world axes**. Negating Z is the standard
choice (it's what FBX/glTF importers do under the hood) because:

- It preserves Y as the up axis (so Y still "feels right" in the viewer).
- It flips the handedness, which is what we need.
- After the flip, the cameras' source `+Z = look-direction` (which mapped to
  world `−Z` in the source) ends up mapping to world `+Z` in Unity — which
  is exactly Unity's camera-forward axis. So cameras "naturally" look into
  the scene in Unity after the Z flip.

So the transformation, in compact form, is:

```
Let S = diag(1, 1, −1, 1).                  # Z-flip in world space.
For each PLY (in its camera-local frame):
    p_world_unity = S · T_cw · p_local      # bake cam→world, then flip Z.
For each traj.txt pose:
    T_unity = S · T_cw · S                  # conjugate by S.
                                            # (S is its own inverse.)
```

Why the conjugation `S · T · S` for the trajectory matrix and not just
`S · T`? Because the matrix lives in `world ← camera` space; multiplying on
the left by `S` flips the *output* world coords, but the input (the camera
frame) is also expressed in the same handedness. Conjugating gives a matrix
that still represents "camera → world", but with both endpoints in the
viewer's coordinate system. The right-multiplication by `S` reflects the
camera's local Z axis at the same time so that the camera's stored
look-direction stays consistent.

**Alternatives we'll keep as a fallback**, in case the Z-flip doesn't render
correctly in the viewer:

- Y-flip (`S = diag(1, −1, 1, 1)`): would imply the source convention was
  Y-down, not Y-up. The cameras' `−0.99` Z component above doesn't really
  support that.
- No flip at all: would mean the source was already left-handed (Unity-style)
  and we only needed the cam→world baking. Possible but unusual for CV data.
- X-flip: very unusual; would mean the camera was mirrored.

If the Z-flip doesn't look right in the viewer, `scripts/transform.py`
accepts a `--flip {x,y,z,none}` CLI flag so the user can try alternates
without editing code.

## 8. Output format choice (ASCII vs. binary PLY)

The originals are ASCII PLY (~125 MB each, slow to load). Most PLY readers
accept either, but custom Unity loaders are sometimes ASCII-only. To
minimise risk of the viewer choking on our output, **we emit ASCII PLY
matching the original layout**: `x y z r g b` per line, with `float` for
position and `uchar` for color. We keep ~6 decimal places — enough to
preserve mm precision at the scene's scale and roughly match the source.
The `transform.py` script also supports `--binary` to emit binary
little-endian PLY for users who care more about load time than parser
compatibility.
